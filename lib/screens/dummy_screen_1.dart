import 'package:flutter/material.dart';

class DummyScreen1 extends StatelessWidget {
  DummyScreen1({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Dummy 1'),
    );
  }
}
